#ifndef OPENCRYPTO_UTILS_H
#define OPENCRYPTO_UTILS_H

#define OPENCRYPTO_TRUE 1
#define OPENCRYPTO_FALSE 0
#define ARRAY_LENGTH(arr) (sizeof(arr) / sizeof(arr[0]))
#define STRING_LENGTH(str) sizeof(str)

#endif